# mystring.cpp 要求
**自定义字符串类 MyString**

## 一、题目要求

编写一个字符串类 `MyString`，并在主函数中进行测试。

要求：

- 不使用字符串库函数。
- 使用字符数组或字符指针存储字符串。

## 二、类的基本信息

- 类名：`MyString`
- 数据成员：字符数组或字符指针

## 三、基础功能

`MyString` 类需要实现以下功能：

1. 构造函数、复制构造函数和析构函数。
2. 打印字符串。
3. 获取字符串长度，不包含字符串结束符 `\0`。
4. 复制一个字符串。
   - 参数可以是 `MyString` 类对象。
   - 参数也可以是普通字符串。
5. 将一个字符串插入到第 `i` 个位置。
   - 参数包括插入位置和要插入的字符串。
   - 要插入的字符串可以是 `MyString` 类对象。
   - 要插入的字符串也可以是普通字符串。
6. 将字符串中的字母全部改成大写字母。
7. 将字符串中的数字按顺序移动到最后。
8. 实现字符串匹配功能。
   - 匹配成功返回 `1`。
   - 匹配不成功返回 `-1`。

## 四、运算符重载功能

在基础功能之外，继续添加以下运算符重载：

1. 重载运算符 `+`：实现两个字符串相连接。
2. 重载前置运算符 `++`：实现字符串向左位移。

   示例：

   ```cpp
   MyString ms = "abcd";
   ++ms; // ms 为 "bcda"
   ```

3. 重载后置运算符 `++`：实现字符串向右位移。

   示例：

   ```cpp
   MyString ms = "abcd";
   ms++; // ms 为 "dabc"
   ```

## 五、选做功能

可选实现以下功能：

1. 重载运算符 `=`：实现赋值功能。
2. 重载运算符 `<<`：实现输出功能。
