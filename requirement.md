# mystring.cpp 要求
**自定义字符串类 MyString**

## 一、题目要求

编写一个字符串类 `MyString`，并在主函数中进行测试。

要求：

- 不使用字符串库函数。
- 使用字符数组或字符指针存储字符串。

## 二、类的基本信息

- 类名：`MyString`
- 数据成员：字符数组或字符指针

## 三、基础功能

`MyString` 类需要实现以下功能：

1. 构造函数、复制构造函数和析构函数。
2. 打印字符串。
3. 获取字符串长度，不包含字符串结束符 `\0`。
4. 复制一个字符串。
   - 参数可以是 `MyString` 类对象。
   - 参数也可以是普通字符串。
5. 将一个字符串插入到第 `i` 个位置。
   - 参数包括插入位置和要插入的字符串。
   - 要插入的字符串可以是 `MyString` 类对象。
   - 要插入的字符串也可以是普通字符串。
6. 将字符串中的字母全部改成大写字母。
7. 将字符串中的数字按顺序移动到最后。
8. 实现字符串匹配功能。
   - 匹配成功返回 `1`。
   - 匹配不成功返回 `-1`。

## 四、运算符重载功能

在基础功能之外，继续添加以下运算符重载：

1. 重载运算符 `+`：实现两个字符串相连接。
2. 重载前置运算符 `++`：实现字符串向左位移。

   示例：

   ```cpp
   MyString ms = "abcd";
   ++ms; // ms 为 "bcda"
   ```

3. 重载后置运算符 `++`：实现字符串向右位移。

   示例：

   ```cpp
   MyString ms = "abcd";
   ms++; // ms 为 "dabc"
   ```

## 五、选做功能

可选实现以下功能：

1. 重载运算符 `=`：实现赋值功能。
2. 重载运算符 `<<`：实现输出功能。

---

# date.cpp 要求
**自定义日期类 Date**

## 一、题目要求

设计一个日期类 `Date`，并在主函数中进行测试。

要求：

- 类中包含年、月、日等私有数据成员。
- 重载 `+` 和 `-` 运算符，实现日期的基本运算。
- 补充完整测试用例，使测试覆盖常见日期运算情况。

## 二、类的基本信息

- 类名：`Date`
- 私有数据成员：
  - 年
  - 月
  - 日

## 三、基础功能

`Date` 类需要实现以下功能：

1. 构造函数，用于初始化年、月、日。
2. 判断某一年是否为闰年。
3. 获取某个月的天数。
4. 日期合法性判断。
5. 打印日期。
6. 实现日期的自增操作。
   - 前置 `++`：日期增加一天。
   - 后置 `++`：日期增加一天，并返回增加前的日期。
7. 实现日期的自减操作。
   - 前置 `--`：日期减少一天。
   - 后置 `--`：日期减少一天，并返回减少前的日期。

## 四、运算符重载功能

在基础功能之外，继续添加以下运算符重载：

1. 重载运算符 `+`：实现一个日期加上指定天数。

   示例：

   ```cpp
   Date d(2015, 2, 2);
   (d + 100).print();
   ```

2. 重载运算符 `-`：实现一个日期减去指定天数。

   示例：

   ```cpp
   Date d(2015, 2, 2);
   (d - 100).print();
   ```

3. 重载运算符 `-`：实现两个日期相减，返回两个日期相差的天数。

   示例：

   ```cpp
   Date d1(2015, 2, 2);
   Date d2(2015, 1, 1);
   cout << d1 - d2 << endl;
   ```

## 五、测试要求

这里给出一个测试用主函数和结果的例子：

```cpp
int main()
{
    Date d(2015, 2, 2);

    ++d;
    d.print();

    (d + 100).print();
    d.print();

    return 0;
}
```

上面的例子只测试了一组数据，没有测试日期减去一天和两个日期相减的情况。请在此基础上补充完整测试用例。

测试内容应包括：

1. 普通日期加一天。
2. 月末日期加一天。
3. 年末日期加一天。
4. 普通日期减一天。
5. 月初日期减一天。
6. 年初日期减一天。
7. 日期加上若干天。
8. 日期减去若干天。
9. 两个日期相减。
10. 跨平年的日期运算。
11. 跨闰年的日期运算。
12. 2 月 28 日、2 月 29 日等特殊日期的运算。

## 六、参考测试方向

可以补充以下测试用例：

1. `Date(2015, 2, 2)` 加一天。
2. `Date(2015, 2, 28)` 加一天，测试平年二月。
3. `Date(2016, 2, 28)` 加一天，测试闰年二月。
4. `Date(2016, 2, 29)` 加一天，测试闰年二月最后一天。
5. `Date(2015, 3, 1)` 减一天，测试平年跨二月。
6. `Date(2016, 3, 1)` 减一天，测试闰年跨二月。
7. `Date(2015, 12, 31)` 加一天，测试跨年。
8. `Date(2016, 1, 1)` 减一天，测试跨年。
9. `Date(2015, 2, 2) + 100`。
10. `Date(2015, 2, 2) - 100`。
11. `Date(2016, 3, 1) - Date(2016, 2, 28)`。
12. `Date(2017, 3, 1) - Date(2017, 2, 28)`。

---

# mammal.cpp 要求
**哺乳动物类 Mammal 及派生类 Dog、Cat**

## 一、题目要求

定义一个哺乳动物类 `Mammal`，并由它派生出狗类 `Dog` 和猫类 `Cat`。

要求：

- `Mammal` 类中包含基本信息，例如姓名 `name` 和年龄 `age`。
- `Dog` 类和 `Cat` 类都需要能够输出自己的叫声。
- 编程实现并测试 `Dog` 类和 `Cat` 类，输出它们的基本信息和叫声。
- 分别尝试将 `Mammal` 设计为普通类和抽象类。

## 二、类的基本信息

- 基类：`Mammal`
- 派生类：
  - `Dog`
  - `Cat`
- 基本数据成员：
  - 姓名 `name`
  - 年龄 `age`

## 三、基础功能

`Mammal` 类需要实现以下功能：

1. 构造函数，用于初始化姓名和年龄。
2. 输出基本信息，例如姓名和年龄。
3. 定义成员函数 `Speak()`，用于输出动物叫声。

`Dog` 类和 `Cat` 类需要实现以下功能：

1. 继承 `Mammal` 类中的基本信息。
2. 重新定义 `Speak()` 函数。
   - `Dog` 类输出狗的叫声。
   - `Cat` 类输出猫的叫声。
3. 在 `Speak()` 函数中可以同时输出该对象的基本信息和叫声。

## 四、普通类实现要求

第一次尝试时，将 `Mammal` 设计为普通类。

要求：

1. `Mammal` 类可以直接创建对象。
2. `Mammal` 类中的 `Speak()` 函数可以给出一个默认输出。
3. 使用基类指针分别指向 `Mammal`、`Dog` 和 `Cat` 对象。
4. 通过基类指针调用 `Speak()`，观察是否能够正确输出不同对象的叫声。

普通类时的参考测试用主函数：

```cpp
int main()
{
    Mammal *m[3];

    m[0] = new Mammal("GO", 4);
    m[1] = new Dog("大黄", 5);
    m[2] = new Cat("小瞄", 3);

    for (int i = 0; i < 3; i++)
    {
        m[i]->Speak();
        delete m[i];
    }

    return 0;
}
```

## 五、抽象类实现要求

第二次尝试时，将 `Mammal` 设计为抽象类。

抽象类是指包含纯虚函数的类。纯虚函数没有在基类中给出完整实现，主要用于规定派生类必须实现的接口。含有纯虚函数的类不能直接创建对象，只能作为基类使用。

要求：

1. 将 `Mammal` 类中的 `Speak()` 定义为纯虚函数。
2. `Dog` 类和 `Cat` 类必须重写 `Speak()` 函数。
3. 不能再直接创建 `Mammal` 对象。
4. 使用基类指针分别指向 `Dog` 和 `Cat` 对象，并调用 `Speak()`。
5. 注意释放动态创建的对象，避免内存泄漏。

抽象类时可以参考以下测试方向：

1. 创建 `Dog("大黄", 5)` 对象并调用 `Speak()`。
2. 创建 `Cat("小瞄", 3)` 对象并调用 `Speak()`。
3. 尝试思考为什么不能执行 `new Mammal("GO", 4)`。

## 六、测试要求

测试内容应包括：

1. 输出 `Dog` 对象的姓名、年龄和叫声。
2. 输出 `Cat` 对象的姓名、年龄和叫声。
3. 对比 `Mammal` 为普通类和抽象类时，主函数写法上的区别。
4. 观察通过基类指针调用 `Speak()` 时，是否体现出多态性。

---

# teacher.cpp 要求
**教师类 Teacher 及不同职称派生类**

## 一、题目要求

某学校教师每月工资的计算公式如下：

```text
月工资 = 固定工资 + 课时数 × 每课时补贴
```

不同职称教师的固定工资和课时补贴如下：

| 职称 | 固定工资 | 每课时补贴 |
| --- | ---: | ---: |
| 教授 | 5000 元 | 50 元 |
| 副教授 | 3000 元 | 30 元 |
| 讲师 | 2000 元 | 20 元 |

要求定义教师基类 `Teacher`，并派生出不同职称的教师类。编写测试程序，计算并输出若干教师的月工资。

## 二、类的基本信息

- 基类：`Teacher`
- 派生类：
  - `Professor`
  - `AssociateProfessor`
  - `Lecturer`
- 基本数据成员：
  - 姓名 `name`
  - 课时数 `hours`

## 三、基础功能

`Teacher` 类需要实现以下功能：

1. 构造函数，用于初始化教师姓名和课时数。
2. 定义工资计算函数，用于计算教师月工资。
3. 定义输出函数，用于输出教师姓名、职称、课时数和月工资。
4. 使用虚函数实现不同职称教师工资计算方式的多态调用。

不同职称的派生类需要实现以下功能：

1. 继承 `Teacher` 类中的基本信息。
2. 根据对应职称设置固定工资和每课时补贴。
3. 重写工资计算函数。
   - 教授：固定工资为 5000 元，每课时补贴为 50 元。
   - 副教授：固定工资为 3000 元，每课时补贴为 30 元。
   - 讲师：固定工资为 2000 元，每课时补贴为 20 元。
4. 输出与该教师职称相匹配的工资结果。

## 四、主函数要求

主函数中创建一个教师类指针数组 `TeaGroup`，用于存放不同类型的教师对象。

要求：

1. 使用 `Teacher` 类指针分别指向 `Professor`、`AssociateProfessor` 和 `Lecturer` 对象。
2. 将这些对象存入指针数组 `TeaGroup`。
3. 遍历 `TeaGroup` 数组，调用其中对象的工资计算函数或输出函数。
4. 观察通过基类指针调用函数时，是否能够得到不同教师对应的工资结果。
5. 注意释放动态创建的对象，避免内存泄漏。

可以参考以下测试方向：

```cpp
int main()
{
    Teacher *TeaGroup[3];

    TeaGroup[0] = new Professor("张老师", 20);
    TeaGroup[1] = new AssociateProfessor("李老师", 18);
    TeaGroup[2] = new Lecturer("王老师", 16);

    for (int i = 0; i < 3; i++)
    {
        TeaGroup[i]->printSalary();
        delete TeaGroup[i];
    }

    return 0;
}
```

## 五、测试要求

测试内容应包括：

1. 输出教授对象的姓名、课时数和月工资。
2. 输出副教授对象的姓名、课时数和月工资。
3. 输出讲师对象的姓名、课时数和月工资。
4. 验证不同职称教师的固定工资和课时补贴是否正确。
5. 观察通过基类指针数组 `TeaGroup` 调用工资计算函数时，是否体现出多态性。
